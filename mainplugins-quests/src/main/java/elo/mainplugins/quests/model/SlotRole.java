package elo.mainplugins.quests.model;

/** Rola pola w menu: kategoria (menu główne), zadanie / przyciski (strona kategorii), tło. */
public enum SlotRole { CATEGORY_SLOT, QUEST_SLOT, NAV_PREV, NAV_BACK, NAV_NEXT, FILLER }
