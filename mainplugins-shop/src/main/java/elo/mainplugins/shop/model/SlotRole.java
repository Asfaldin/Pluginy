package elo.mainplugins.shop.model;

/** Co leży na polu okna sklepu. ROTATION_SLOT = miejsce na wylosowany przedmiot z rotacji (strona kategorii). */
public enum SlotRole { CATEGORY_SLOT, ITEM_SLOT, ROTATION_SLOT, AMOUNT_SLOT, NAV_BACK, NAV_PREV, NAV_NEXT, EXIT, SEARCH, SORT, FILLER }
